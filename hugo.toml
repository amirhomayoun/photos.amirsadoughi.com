baseURL = 'https://photos.amirsadoughi.com/'
languageCode = 'en-us'
title = 'Amir Sadoughi - Photography'

[params]
  description = "Personal photography portfolio"
  author = "Amir Sadoughi"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true  # Allow raw HTML in markdown if needed

# Disable unused content types to silence warnings
disableKinds = ['taxonomy', 'term', 'section']
